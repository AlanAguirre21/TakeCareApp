<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (modo claro forzado). -->
    <style name="Base.Theme.TakeCare" parent="Theme.Material3.Light.NoActionBar">
        <!-- Personaliza colores si quieres -->
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="Theme.TakeCare" parent="Base.Theme.TakeCare" />
</resources>
